[service]
name = "orchestrator"

[build]
builder = "NIXPACKS"
buildPath = "../orchestrator"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[deploy.resources]
memory = "512MB"
cpu = 1
